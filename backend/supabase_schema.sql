-- Optional reference DDL for Supabase SQL editor.
-- The app also runs SQLAlchemy Base.metadata.create_all() on startup.
-- If you already have old tables, drop or migrate columns manually before first run.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
